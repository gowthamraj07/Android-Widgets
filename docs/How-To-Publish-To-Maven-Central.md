### Use the following command to publish library to Maven Central
`./gradlew clean publish --no-daemon --no-parallel`

### Use the following command to release the library (to production)
`./gradlew closeAndReleaseRepository`